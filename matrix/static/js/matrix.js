$(document).ready(function(){
	renderApp();
});

function renderApp(){
	$("#app").html("<textarea rows=\"20\" cols=\"20\"></textarea>" 
    + "<h1>Tester chester</h1>"
    + "Not so bad eh. Cool it's all working great now!");
}

renderApp();